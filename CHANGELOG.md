# 更新记录

## 0.2.1 - 2026-08-15

- 增加 Windows 单文件便携版 EXE，下载后无需解压即可直接运行；
- 保留 Windows 文件夹便携版 ZIP，并继续提供无需管理员权限的安装版；
- 增加 macOS `.app` 直接运行分享 ZIP，同时保留 DMG 安装包；
- GitHub Release 自动上传全部平台产物，并为每个产物生成 SHA-256 校验值。

- 补充直播名场面语录，保留原始口音和写法；新增内容强度分类、情绪映射和托盘开关，默认开启并支持持久化配置。
- 增加语录模式回归测试，覆盖新增语录加载、普通/成人语录池隔离、情绪映射和成人模式开关。
- 重做语录气泡 UI：按情绪切换手绘贴纸配色，优化长句换行、圆润尾巴和阴影，并增加轻量淡入淡出动画；成人语录保持原文显示且不在气泡内标注。
- 增加气泡 UI 离屏测试，覆盖主题、排版、成人模式状态、尾巴方向、动画和 PetWindow 接入。
- 修正气泡锚点：避开 Classic/Q 版透明画布的上下安全留白，改为贴近宠物实际可见区域。
- 二次修复动画黑边与贴边裁切：对 Classic/Q 版全部运行时帧执行透明边缘去杂色，补充四周安全画布留白，动作安装器继承 idle 的脚底边距，并限制拖拽、跳跃和自动移动不越出屏幕。
- 针对残留浅色外轮廓和跳跃帧顶部裁切再次收口：运行时帧统一收紧两层透明边界，改用 `389/393×444` 安全画布，确保顶部至少 48px、底部 16px、左右至少 16px 留白；动作安装器同步限制横向宽度。
- 修复打包版资源路径在 macOS PyInstaller 包中的回退问题：按实际包含 `assets/quotes.txt` 的 Resources/`_internal` 目录解析资源，避免皮肤目录未找到时误加载旧版 `assets/`。
- 修复 macOS 透明宠物/气泡窗口的运行时黑边：关闭 AppKit 沿透明 alpha 轮廓生成的原生 NSWindow 阴影，避免白色背景上出现素材本身不存在的深色描边。
- 修复 macOS 未安装 PyObjC 时快捷键映射常量未定义的问题；原生全局监听仍按能力降级，纯 Python 映射和配置校验可正常运行。
- 将托盘语录开关调整为三类内容策略：成人专属、普通专属和双模式共用；成人模式不再与普通模式完全互斥，并修复长气泡靠近屏幕边缘时的完整显示。
- 修复长语录气泡的水平裁切：统一换行宽度与实际文字绘制区域，避免最后一个字被排入行内后又被右侧边界截断；新增截图同款长句回归测试。
- 同步语录审阅结果：普通专属 59 条、双模式共用 41 条、成人专属 19 条；新增“管理语录库”表格，可新增、编辑、停用/启用、删除语录并实时应用到当前宠物。
- 重做 Classic / Q 版的 walk、run、angry、sad、cry、spin、sing、shy 动作：补齐步态、旋转、情绪和害羞/歌唱变化，移除旧帧中误带的骨头与心形，避免动作语义串台；同步补齐 15 个状态的帧数规格和 idle 回退防回归测试。
- 修复动作条拆帧时相邻面板残片、透明背景光晕和画布边缘裁切问题；统一所有动作的画布、可见高度与脚底基线，避免 `happy`、跳跃等状态切换时出现明显缩放跳变。
- 补强 macOS 顶部边界保护：窗口、跳跃、启动恢复和位置复位统一保留顶部安全距离，避免原生窗口合成时偶发裁掉宠物最顶部。
- 修复放大动画帧在负坐标粘贴时丢失顶部像素的问题；重建两套皮肤的待机/跳跃帧，并增加顶部轮廓连续性回归检查。
- 增加 `tests/test_animation_assets.py`，并接入 CI，自动检查 Classic/Q 版全部状态的画布尺寸、边缘安全、背景泄漏和 `happy` 基线稳定性。
- 完成 macOS arm64 `.app` 与 DMG 的本机验收：干净启动、happy 互动和双击喂食动作通过验证，代码签名与 DMG 校验通过。
- 重做 Classic / Q 版的喂食、跳舞、坐下、睡觉动作：喂食骨头贴合嘴部，跳舞改为抬爪与重心移动，坐下改为真实坐姿，睡觉改为闭眼蜷卧；同步加入动作条拆帧、透明背景清理、边界裁切和道具连通性检查。
- 重新设计 Classic / Q 版的 `happy` 互动动画：单击、长按和连续点击使用抬爪、眨眼与尾巴回应，不再复用旧版互动帧，也不与喂食骨头动作混淆。
- 增加经典高清版与 Q 版两套独立宠物皮肤，各包含 15 种状态和 68 帧动画；
- 增加宠物右键菜单和托盘菜单的形象切换，并持久化每只宠物的 `appearance`；
- 更新应用图标标准图，并重新导出 macOS `.icns`、Windows `.ico` 和运行时 PNG；
- 素材生成器支持通过 `NANA_ASSETS_DIR` 为不同皮肤生成独立动画资源；
- 增加第一轮角色、图标视觉概念与验收素材。

## 0.2.0 - 2026-08-14

- 增加 `75%～200%` 连续缩放、实时滑块预览和侧边弹出大小设置窗口；
- 修复移除最后一只宠物时自动重生的问题，支持运行期间暂时保持 0 只宠物；
- 完善无宠物时的托盘菜单状态，并保留重启时自动创建默认宠物的行为；
- 增加旧版大小配置迁移、快捷键重复检测和动画帧缩放缓存；
- 增加多宠物、无宠物、重启恢复、缩放和快捷键回归测试。

## 0.1.1 - 2026-08-14

- 拆分宠物数据、对话气泡、全局快捷键和设置对话框模块，同时保留 `main.py`、`pet.py` 的兼容入口；
- 增加 GitHub Actions 的跨平台测试、macOS arm64 和 Windows 自动构建工作流；
- 增加推送版本标签后自动创建 GitHub Release 的发布流程。

## 0.1.0 - 2026-08-14

### 功能

- 支持多只桌宠、动画状态、语录气泡、喂食、拖拽和位置持久化；
- 支持 Windows 托盘控制与全局快捷键；
- 支持 macOS 菜单栏运行、原生浮动窗口层级和 Quartz 全局快捷键；
- 支持 macOS `.app` 与 DMG 打包。

### 修复与适配

- 修复 macOS 切换应用后宠物窗口位于其他窗口后方的问题；
- 增加“显示/恢复全部宠物”入口；
- 增加 macOS 快捷键权限状态提示；
- 完善 Windows/macOS 双平台资源路径和构建路径。

### 工程整理

- 测试、素材工具和平台打包文件按职责分目录；
- 增加构建依赖文件和素材发布说明；
- 统一普通项目文件权限，仅保留 macOS 构建脚本可执行。
