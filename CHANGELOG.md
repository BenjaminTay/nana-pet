# 更新记录

## 0.2.0 - 2026-08-14

- 增加 `75%～200%` 连续缩放、实时滑块预览和侧边弹出大小设置窗口；
- 修复移除最后一只宠物时自动重生的问题，支持运行期间暂时保持 0 只宠物；
- 完善无宠物时的托盘菜单状态，并保留重启时自动创建默认宠物的行为；
- 增加旧版大小配置迁移、快捷键重复检测和动画帧缩放缓存；
- 增加多宠物、无宠物、重启恢复、缩放和快捷键回归测试。

## 0.1.1 - 2026-08-14

- 拆分宠物数据、对话气泡、全局快捷键和设置对话框模块，同时保留 `main.py`、`pet.py` 的兼容入口；
- 增加 GitHub Actions 的跨平台测试、macOS arm64 和 Windows 自动构建工作流；
- 增加推送版本标签后自动创建 GitHub Release 的发布流程。

## 0.1.0 - 2026-08-14

### 功能

- 支持多只桌宠、动画状态、语录气泡、喂食、拖拽和位置持久化；
- 支持 Windows 托盘控制与全局快捷键；
- 支持 macOS 菜单栏运行、原生浮动窗口层级和 Quartz 全局快捷键；
- 支持 macOS `.app` 与 DMG 打包。

### 修复与适配

- 修复 macOS 切换应用后宠物窗口位于其他窗口后方的问题；
- 增加“显示/恢复全部宠物”入口；
- 增加 macOS 快捷键权限状态提示；
- 完善 Windows/macOS 双平台资源路径和构建路径。

### 工程整理

- 测试、素材工具和平台打包文件按职责分目录；
- 增加构建依赖文件和素材发布说明；
- 统一普通项目文件权限，仅保留 macOS 构建脚本可执行。
